---
---

<style>
  h3, p {
    text-align: center;
  }

  p {
    font-size: var(--text-lg);
    color: var(--sl-color-gray-2);
  }

  .feature-wrapper {
    width: 100%;
    background: linear-gradient(to bottom, #0f0f11 0%, #202124 60%, #0f0f11);
  }

  .feature-showcase {
    max-width: 1100px;
    margin: 0 auto;
    padding: 4rem 1rem;
    /* background: linear-gradient(to bottom, #0f0f11, #202124 60%, #17181c); */
  }

  .feature-showcase h2 {
    text-align: center;
    margin-bottom: 3rem;
    font-size: var(--text-4xl);
  }

  .feature-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .feature-row.reverse {
    direction: rtl;
  }

  .feature-row.reverse .feature-content {
    direction: ltr;
  }

  .feature-image img {
    width: 100%;
    max-width: 500px;
    border: 1px solid var(--sl-color-hairline-shade);
    border-radius: 0.5rem;
  }

  .feature-content h3 {
    font-size: var(--text-2xl);
    font-weight: bold;
    margin-bottom: 0.5rem;
  }

  @media (max-width: 768px) {
    .feature-row,
    .feature-row.reverse {
      grid-template-columns: 1fr;
    }

    .feature-row.reverse {
      direction: ltr;
    }

    .feature-image {
      display: flex;
      justify-content: center;
    }

    .feature-image img {
      width: 100%;
      max-width: 90%;
    }
  }
</style>

<div class="feature-wrapper">
<section class="feature-showcase">
  <h2>Find What You’re <span class="highlight">Really</span> Looking For</h2>

  <!-- Query By Text -->
  <div class="feature-row">
    <div class="feature-image">
      <img src="/assets/search-by-text.gif" alt="Natural Language Search" />
    </div>
    <div class="feature-content">
      <h3>Query with <span class="highlight">Natural Language</span></h3>
      <p>
        Watch as Frame transforms complex queries into precise visual results — powered by real-time vector embeddings and our lightweight, self-hosted architecture.
      </p>
    </div>
  </div>
  <!-- Query By Image -->
  <div class="feature-row reverse">
    <div class="feature-image">
      <img src="/assets/search-by-image.gif" alt="Reverse Image Search" />
    </div>
    <div class="feature-content">
      <h3><span class="highlight">Reverse</span> Image Search</h3>
      <p>
        Upload a photo to uncover visually and semantically similar results — whether it’s near-duplicates, related styles, or thematically linked content.
      </p>
    </div>
  </div>

  <!-- Recommendation -->
  <div class="feature-row">
    <div class="feature-image">
      <img src="/assets/recommendations.gif" alt="Smart Recommendations" />
    </div>
    <div class="feature-content">
      <h3>Smart <span class="highlight">Recommendations</span></h3>
      <p>
        Whether you’re exploring artwork, researching topics, or browsing product catalogs, Frame helps surface “more like this” results that actually feel relevant.
      </p>
    </div>
  </div>
</div>
</section>
